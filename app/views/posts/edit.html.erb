<h1>Edit Page</h1>

<div class="container">
    <form action="/update" methond="get"> <!-- veiw에 action이 걸리면 controller에 있는 update로 넘어간다-->
        <div class="form-group">
            <label for="title">Title</label>
            <input type="name" value="<%= @edit_post.title %>" name="title" class="form-control" id="NameInputEmail1">
        </div>
          
        <dciv class="form-group">
            <label for="body">Body</label>
            <input type="name" value="<%= @edit_post.body %>" name="body" class="form-control" id="NameInputEmail1">
        </div>
        <textarea name="id" style="display:none;"><%= @edit_post.id %></textarea>
        <button type="submit">Update</button>
    </form>
</div>

